extends: distribution_carm_10pct_v2

experimental:
  run_name: RUN-EXP-dist_preflight_v2
  output_root: outputs/experimental
  random_seed: 7
  sampling_strategy: protocol_category_family
  max_train_examples: 15
  max_val_examples: 15
  max_test_examples: 15
  training:
    batch_size: 1
    epochs: 2
    lr: 0.001
    patience: 2
    log_every_steps: 5

runtime:
  notes: Preflight for distribution CARM v2 — validates action_class_weights pipeline on 15-example subset.
