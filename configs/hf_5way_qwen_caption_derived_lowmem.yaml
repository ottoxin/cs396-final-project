extends: hf_5way_qwen_caption_derived

backbone:
  cache_results: true
  cache_max_entries: 512

training:
  batch_size: 1
  log_every_steps: 100

runtime:
  notes: Low-memory CARM runtime on the HF-aligned caption-derived export with bounded backbone caches.
